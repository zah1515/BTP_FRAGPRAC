_schema-version: "3.1"
ID: Fragmentprac
description: A simple CAP project.
version: 1.0.0
modules:
- name: Fragmentprac-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: Fragmentprac-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm
- name: Fragmentprac-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: Fragmentprac-db
  parameters:
    buildpack: nodejs_buildpack
- name: Fragmentprac-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: Fragmentprac-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - fragpraclist.zip
      name: fragpraclist
      target-path: resources/
    - artifacts:
      - operation.zip
      name: operation
      target-path: resources/
- name: fragpraclist
  type: html5
  path: app/fragpraclist
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
- name: operation
  type: html5
  path: app/operation
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: Fragmentprac-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: Fragmentprac-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: Fragmentprac-html5-srv
    service-plan: app-host
- name: Fragmentprac-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: Fragmentprac-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx cds build --production
